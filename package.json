{
  "name": "zapflow-disparo-teste",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal PoC to send ONE approved WhatsApp template message via Umbler Talk.",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc --noEmit",
    "send:test": "tsx src/send-test-template.ts",
    "send:payload": "tsx src/send-from-payload.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.0.0",
    "dotenv": "^16.4.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
